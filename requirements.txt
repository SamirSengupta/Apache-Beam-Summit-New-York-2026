# Required for the demo
apache-beam==2.66.0   # the streaming pipeline engine
numpy>=1.24           # vector math for the embedder

# Optional (the demo auto-falls back if these are missing)
faiss-cpu>=1.7        # real vector database for RAG; numpy fallback used if absent

# Notes:
# - The LLM is called with Python's built-in urllib, so NO 'openai' package is
#   needed. Nothing here talks to the cloud.
# - To use real semantic embeddings instead of the built-in hashing embedder,
#   add:  sentence-transformers>=2.2   (and swap the HashingEmbedder class).
